#include <Geode/Geode.hpp>
#include <Geode/modify/GJUserScore.hpp>

// class $modify(StreakSetter, GJUserScore) {
//     bool init() {
//         if (!GJUserScore::bool init()) return false;
//         if (isCurrentUser()) {
//             this->m_playerStreak = GameManager::get()->getPlayerStreak();
//             // log::debug("streak setting as {}", this->m_playerStreak);
//         }
//     }
// };
